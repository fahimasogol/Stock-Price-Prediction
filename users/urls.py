from django.urls import path

# from rest_framework import routers
# from .views import CustomUserViewSet, SetAlertAPIView, test_view


urlpatterns = [
    # path('set-alert/', SetAlertAPIView.as_view(), name='set-alert'),
    # path('test/', test_view, name='test'),
]
